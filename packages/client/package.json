{
  "name": "client",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "build": "rollup -c && rm -rf ../pikav/dist/client && mkdir -p ../pikav/dist && mv dist ../pikav/dist/client"
  },
  "license": "Apache-2.0",
  "devDependencies": {
    "rollup": "^3.23.0",
    "rollup-preset-solid": "^2.0.1",
    "solid-js": "^1.7.5",
    "typescript": "^5.0.4"
  }
}
